/**
 * HTTPS origin of the ParkWalk API on Railway (no path, no trailing slash).
 *
 * `API_BASE_URL` in mobile/.env overrides this when non-empty (local dev).
 */
export const RAILWAY_PUBLIC_API_ORIGIN = 'https://parkwalk-production.up.railway.app';
